# zyGD修改器API与功能分析文档

> **状态：静态分析参考，非已验证接口。** 请将本文中的功能描述视为推测；已核实的证据、范围和限制见 [`docs/zygd_static_analysis.md`](docs/zygd_static_analysis.md)。本项目后续开发不依赖本文推测。

## 📋 **文档说明**

基于对zyGD_v2.1.1a_x64.exe程序使用Windows API的分析，推测其内部实现原理和功能架构。

---

## 🔍 **Windows API使用分析**

### **1. 进程管理类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `CreateToolhelp32Snapshot` | 创建系统快照 | 扫描并定位Grim Dawn.exe进程 |
| `Process32FirstW` | 枚举进程（第一个） | 开始遍历进程列表 |
| `Process32NextW` | 枚举进程（下一个） | 继续查找目标进程 |
| `OpenProcess` | 打开进程获取句柄 | 获取目标进程操作权限 |
| `IsWow64Process` | 检查进程架构 | 确认目标进程是否为64位 |
| `TerminateProcess` | 终止进程 | 异常情况下的强制关闭 |
| `GetExitCodeThread` | 获取线程退出码 | 等待注入操作完成 |

**实现流程：**

进程注入流程：

1. 创建系统快照 → 2. 枚举查找进程 → 3. 打开进程获取权限
2. 验证进程架构 → 5. 执行注入操作 → 6. 等待操作完成
### **2. 内存操作类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `VirtualAllocEx` | 在目标进程分配内存 | 注入DLL路径的存储 |
| `VirtualFreeEx` | 释放目标进程内存 | 清理注入相关内存 |
| `WriteProcessMemory` | 写入目标进程内存 | 注入代码和数据 |
| `ReadProcessMemory` | 读取目标进程内存 | 获取游戏内存数据 |
| `VirtualQueryEx` | 查询目标进程内存 | 分析内存区域布局 |
| `CreateFileMappingW` | 创建文件映射 | 建立内存映射区域 |
| `MapViewOfFile` | 映射文件到内存 | 访问游戏数据库 |
| `UnmapViewOfFile` | 解除内存映射 | 释放映射资源 |

**实现流程：**内存读写流程：

1. 创建文件映射 → 2. 映射到进程内存 → 3. 读取游戏数据
2. 修改数据内容 → 5. 写入修改结果 → 6. 解除映射释放


### **3. DLL注入类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `CreateRemoteThread` | 创建远程线程 | 在目标进程执行代码 |
| `LoadLibraryW` | 加载动态链接库 | 注入DLL的主入口点 |
| `GetModuleHandleW` | 获取模块句柄 | 查找已加载的模块 |
| `GetProcAddress` | 获取导出函数 | 调用DLL中的特定函数 |
| `FreeLibrary` | 释放动态链接库 | 清理注入的DLL资源 |

**DLL注入实现：**- 
分配内存空间（VirtualAllocEx）
- 写入DLL路径（WriteProcessMemory）
- 创建远程线程（CreateRemoteThread）
- 传入LoadLibraryW作为入口点
- 执行DLL加载和初始化

### **4. GUI界面类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `CreateWindowExW` | 创建扩展窗口 | 主窗口和子控件创建 |
| `SetWindowTextW` | 设置窗口文本 | 更新窗口标题和控件文本 |
| `ShowWindow` | 显示/隐藏窗口 | 控制界面可见性 |
| `UpdateWindow` | 更新窗口 | 强制重绘界面内容 |
| `SetForegroundWindow` | 置顶窗口 | 将窗口设为前台显示 |
| `GetWindowTextW` | 获取窗口文本 | 读取用户输入内容 |
| `EnableWindow` | 启用/禁用窗口 | 控件状态管理 |
| `SendMessageW` | 发送消息 | 控件交互处理 |
| `PostMessageW` | 投递消息 | 异步消息处理 |
| `GetMessageW` | 获取消息 | 消息循环处理 |

**GUI界面实现：**- 创建主窗口（CreateWindowExW）
- 创建子控件（按钮、列表、输入框等）
- 显示窗口（ShowWindow）
- 进入消息循环（GetMessageW）
- 处理用户交互（SendMessageW）
- 
### **5. 系统内核类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `CreateThread` | 创建线程 | 后台数据处理线程 |
| `WaitForSingleObject` | 等待对象 | 同步操作控制 |
| `CloseHandle` | 关闭句柄 | 资源清理管理 |
| `GetLastError` | 获取错误码 | 错误诊断和调试 |
| `SetErrorMode` | 设置错误模式 | 错误显示控制 |
| `GetSystemInfo` | 获取系统信息 | 系统兼容性检查 |

### **6. 时间控制类API**

| API函数 | 功能描述 | 程序用途推测 |
|---------|---------|-------------|
| `GetTickCount` | 获取系统时间戳 | 计时和超时控制 |
| `Sleep` | 暂停执行 | 操作间隔控制 |
| `SetTimer` | 设置定时器 | 周期性任务触发 |
| `KillTimer` | 删除定时器 | 清理定时器资源 |

---

## 🎮 **推测实现的功能模块**

### **1. 物品生成系统**

#### **核心功能：**
- 物品ID修改和替换
- 物品稀有度控制（普通/魔法/稀有/史诗/传奇）
- 物品等级调整
- 属性数值修改
- 前缀/后缀词缀添加
- 物品数量控制
- 耐久度修改

#### **实现推测：**
- 定位游戏内存中的物品结构
- 读取当前物品数据
- 修改物品属性数值
- 添加/替换词缀组合
- 计算最终属性加成
- 写入修改后的数据
- 强制游戏刷新显示

### **2. 角色属性系统**

#### **核心功能：**
- 基础属性调整（力量/敏捷/智力/精力）
- 生命值/法力值修改
- 伤害/防御数值控制
- 各种抗性调整（混乱/以太/元素/火焰/冰冷/闪电/酸/毒等）
- 经验值倍率调整
- 技能点重分配
- 技能等级修改
- 冷却时间调整

#### **实现推测：**
- 定位角色数据内存区域
- 读取当前属性数据
- 修改指定属性数值
- 重新计算衍生属性
- 更新技能相关数据
- 写入修改后的角色数据

### **3. 实时监控系统**

#### **核心功能：**
- 物品变化实时监控
- 属性变化跟踪
- 经验值获取监控
- 掉落物品检测
- 战斗数据统计
- 状态异常检测

#### **实现推测：**

- 建立内存监控线程
- 定期扫描内存变化
- 记录数据变化历史
- 分析变化趋势
- 更新界面显示

### **4. 自动化脚本系统**

#### **核心功能：**
- 自动拾取物品
- 自动使用技能
- 自动移动/传送
- 自动完成任务
- 刷怪副本自动化
- 材料自动收集
- NPC交互自动化

#### **脚本结构推测：**

ScriptCommand {  
command_id: 操作类型  
target_addr: 目标地址  
value: 参数值  
duration: 执行时间  
}


### **5. 数据管理功能**

#### **核心功能：**
- 用户配置保存/加载
- 快捷键配置
- 默认参数设置
- 自定义模板管理
- 数据导出/导入
- 错误日志记录

#### **实现推测：**
- 配置文件读写（JSON/INI格式）
- 注册表配置管理
- 配置验证机制
- 配置备份功能

---

## 🎯 **与现有修改器的技术对比**

### **技术架构差异：**

| 方面 | zyGD推测架构 | 现有修改器架构 |
|------|-------------|---------------|
| **实现语言** | 原生C++ | Python + C++混合 |
| **部署方式** | 单文件静态链接 | 多文件依赖 |
| **内存访问** | 直接API调用 | 命名管道通信 |
| **性能表现** | 高性能，低延迟 | 跨语言通信开销 |
| **开发效率** | 开发周期长 | 快速迭代 |
| **可扩展性** | 闭源限制 | 开源可扩展 |

### **功能实现对比：**

| 功能模块 | zyGD推测能力 | 现有修改器能力 |
|---------|-------------|---------------|
| **物品生成** | 实时修改，支持复杂属性 | 基础生成功能 |
| **属性调整** | 直接内存修改，实时生效 | 通过通信修改 |
| **自动化** | 内置脚本引擎 | 无自动化功能 |
| **监控** | 实时内存监控 | 无监控功能 |
| **UI响应** | 原生界面，响应迅速 | Tkinter界面，有延迟 |
| **稳定性** | 原生API，稳定性好 | 跨语言通信，有异常 |

---

## 🔧 **关键实现技术要点**

### **1. 进程注入核心技术**

```cpp
// 关键步骤
1. 进程定位 - CreateToolhelp32Snapshot()
2. 权限获取 - OpenProcess()
3. 内存分配 - VirtualAllocEx()
4. 数据写入 - WriteProcessMemory()
5. 远程执行 - CreateRemoteThread()
6. 清理工作 - VirtualFreeEx()
   
### **2. 内存读写核心技术**
// 核心操作
1. 内存映射 - CreateFileMappingW()
2. 数据读取 - ReadProcessMemory()
3. 数据修改 - 直接内存操作
4. 数据写入 - WriteProcessMemory()
5. 资源释放 - UnmapViewOfFile()
**3. GUI界面核心技术**
// 界面实现
6. 窗口创建 - CreateWindowExW()
7. 控件生成 - 多次CreateWindowExW()
8. 消息循环 - GetMessageW()
9. 事件处理 - SendMessageW()
10. 状态更新 - SetWindowTextW()
    
    4. 实时监控核心技术
       // 监控机制
1. 内存扫描 - VirtualQueryEx()
2. 数据对比 - ReadProcessMemory()
3. 变化检测 - 内存数据比对
4. 界面更新 - PostMessageW()
5. 线程管理 - CreateThread()
